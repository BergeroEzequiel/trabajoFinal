<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/util
		http://www.springframework.org/schema/util/spring-util-2.0.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd">
    <task:annotation-driven />
    <context:component-scan base-package="ar.edu.ucc.trabajoFinal" />
        
    <!--Estos beans son @Configurable, por eso necesitamos declararlos aca-->
    <bean id="variablesConfiguracion" class="ar.edu.ucc.trabajoFinal.utils.VariablesConfiguracion"/>
    
    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="location">
            <value>app.properties</value>
        </property>
    </bean>
    
    <!--#Configuración del servicio de Spring: MailSernder -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="javaMailProperties">
            <props>
                    <prop key="mail.smtp.starttls.enable">true</prop>
                    <prop key="mail.smtp.auth">true</prop>
                    <prop key="mail.smtps.ssl.checkserveridentity">true</prop>
                    <prop key="mail.smtps.ssl.trust">*</prop>
            </props>
        </property>
        <property name="host" value="${mail.host}"/>
        <property name="port" value="${mail.port}"/>
        <property name="username" value="${mail.username}"/>
        <property name="password" value="${mail.password}"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>
    
    <bean id="mailService" class="ar.edu.ucc.trabajoFinal.utils.MailService">
        <property name="mailSender" ref="mailSender"/>
    </bean>
    
    <!--Bean correspondiente al subscriber de MQTT.
        Los defino aca solo para poder pasarle las properties sacadas del archivo de configuracion.-->
    <bean id="subscriber" class="ar.edu.ucc.trabajoFinal.eclipsePaho.Subscriber">
        <property name="broker" value="${mqtt.client}" />
        <property name="topic" value="${mqtt.topic}" />
        <property name="clientId" value="${mqtt.clientId}" />
        <property name="baseUrl" value="${config.contextPath}" />
    </bean>
    
</beans>